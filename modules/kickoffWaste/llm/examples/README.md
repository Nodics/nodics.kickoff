# Kickoff Waste Examples

Use `data/core-v001` as the minimal project overlay example: it adds a smart-home category, adds a mall drop-off preset, and overrides the standard eWaste drop-off preset with a Kickoff-specific impact profile.
