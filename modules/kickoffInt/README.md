# kickoffInt Module

`kickoffInt` owns project integration customizations for `nodics.kickoff`.

It is activated explicitly by `platformServer`; the containing `modules/`
group participates only in structural discovery.
