# Partner Site Data Agent Contract

- Follow the Nodics Kickoff root and `modules/AGENTS.md` contracts.
- This module demonstrates a later-loaded partner-owned website customization.
- Never modify or assume ownership of Framework, Axis, Nexus frontend, or the
  original `nexusData` release.
- Reuse CMS, import, publishing, workflow, and BackOffice contracts through
  configuration and immutable data releases only.
- Publishable files belong under `data/staged` and must be declared exactly once
  in the module manifest with `WCMS_STAGED` destination ownership.
